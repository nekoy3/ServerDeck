# Python virtual environment
venv/

# Byte-compiled / optimized / DLL files
__pycache__/
*.pyc
*.pyd
*.pyo

# Editor files
.vscode/
.idea/

# Flask specific
instance/
.flaskenv

# OS generated files
.DS_Store
thumbs.db

# Logs and databases
*.log
*.db

# Sensitive information (e.g., passwords in config files)
# IMPORTANT: servers.yaml contains sensitive data. Consider encrypting it or using environment variables.
# For now, we will NOT ignore config/servers.yaml as it's part of the project structure.
# If you decide to move sensitive data out of it, you might want to add it here.

# Ignore actual config files, use .template files for version control
config/servers.yaml
config/ssh_keys.yaml

# Ignore uploaded SSH keys and generated local keys
config/uploaded_ssh_keys/
new_rsa_key
new_rsa_key.pub
new_rsa_key_sha2
new_rsa_key_sha2.pub
id_vm_machines
id_vm_machines.pub
